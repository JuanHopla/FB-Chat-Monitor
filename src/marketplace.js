// ----- REDIRECTION TO MARKETPLACE -----

    // ----- MARKETPLACE FUNCTIONS -----
    // This file contains marketplace-specific functions

    // Function is disabled to prevent automatic redirections
    // The user should navigate directly to the desired URL
    function redirectToMarketplace() {
      // Always return false to prevent redirection
      return false;

      // Original code is commented out to maintain file structure
      // without causing redirections
      /*
      if (window.location.hostname === 'www.messenger.com' &&
          !window.location.pathname.includes('/marketplace')) {
        logger.log('Redirecting to the Marketplace section...');
        window.location.href = 'https://www.messenger.com/marketplace/';
        return true;
      }
      return false;
      */
    }